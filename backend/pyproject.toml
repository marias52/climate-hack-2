[project]
name = "ecorestore-backend"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.111.0",
  "uvicorn[standard]>=0.30.0",
]

[tool.uv]
dev-dependencies = [
  "ruff>=0.5.0",
]
